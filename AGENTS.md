# Codex 구현 지침

이 파일은 Codex가 이 저장소에서 코드를 고칠 때 읽는다. 규칙 원본은 [CLAUDE.md](CLAUDE.md)와 [.claude/rules/](.claude/rules/)에 있고, 여기에 복제하지 않는다.

## 작업 전에 읽을 것

1. 루트 `CLAUDE.md`와 `.claude/rules/` 중 이번 변경에 걸리는 것.
2. 고칠 파일의 상위 디렉터리에 있는 `CLAUDE.md`(`backend/`, `frontend/`)와 그 폴더의 `.claude/`. 더 구체적인 영역 규칙이 이긴다.
3. 규칙 파일 프론트매터에 `paths`가 있으면 변경 경로와 직접 대조해 적용 여부를 판단한다. 경로별 자동 로드는 Codex에서 돌지 않는다.
4. 영역 문서가 가리키는 컨벤션과 ADR 중 이번 변경에 관련된 것만. 탐색에서 `.claude/worktrees/`, 의존성 디렉터리, 빌드 산출물은 뺀다.

빌드·테스트 명령은 `backend/CLAUDE.md`와 `frontend/CLAUDE.md`에 있다.

## 규칙 문서의 주어

**규칙 문서가 Claude를 대상으로 쓴 의무·금지·절차는 Codex에도 적용된다.** 이 저장소에서 코드를 고치는 에이전트 전부가 대상이다. `git-push-safety.md`에는 "이 규칙은 Claude의 git 조작에만 적용된다"는 문장이 있다. 그 한정은 사용자의 수동 push와 대비하려고 쓴 것이라 Codex를 빼주지 않는다. `dev`·`prod`·`main`·`master` 직접 push와 commit, force push, `gh pr merge`가 모두 금지다. 전문은 [git-push-safety](.claude/rules/git-push-safety.md).

## Claude Code 기능은 여기서 돌지 않는다

슬래시 명령·훅·플러그인·자동 로드가 실행된다고 가정하지 않는다. 절차가 필요하면 `.claude/skills/<이름>/SKILL.md`나 `backend/.claude/skills/<이름>/SKILL.md`를 읽고 가진 도구로 수행한다(`write-tests`, `run-tests`, `commit`, `ci-fix`). 도구 이름이 달라도 검증과 안전 조건은 지킨다. 실행할 수 없는 검증은 이유와 함께 보고한다.

## 계획과 구현 범위

받은 계획이 구현 기준이다. 이슈의 `🧭 설계 방향`과 `🔧 TODO`, 성공 기준, 또는 요청에 적힌 계획을 그대로 따른다. 이미 정한 설계를 다시 제안하지 않고, 요청 범위 밖 리팩터링을 얹지 않는다. `CODEX_PLAN.md` 같은 별도 계획 문서나 작업 일지를 만들지 않는다.

별도 계획이 없으면 요청과 기존 코드 패턴을 기준으로 진행한다. 사용자가 명시적으로 요청한 변경은 승인된 범위로 보고, 그 범위를 벗어나는 설계 결정만 질문한다.

이름 짓기, 작은 함수 분리, 테스트 픽스처처럼 계획 안의 세부는 기존 패턴에 맞춰 판단하고 진행한다.

계획이 실제 코드나 ADR과 어긋나거나, 요청에 없던 공개 API·데이터 모델·모듈 경계·의존성·성공 기준 변경이 필요해지면 그 부분만 멈추고 근거와 필요한 결정을 짧게 보고한다. 독립적으로 되는 구현과 검증은 계속한다.

## 보고

변경 내용, **실제로 돌린** 검증과 그 결과, 미해결 사항을 적는다. 돌리지 않은 검사를 통과했다고 쓰지 않는다. 설계 판단이 남았으면 이어받을 사람이 볼 파일 위치와 근거를 함께 적는다. 구현이 끝난 것을 merge나 배포 허가로 해석하지 않는다.
