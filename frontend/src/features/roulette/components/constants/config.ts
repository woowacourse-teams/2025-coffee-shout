export const WHEEL_CONFIG = {
  SIZE: 300,
  CENTER: 150,
  RADIUS: 140,
  TEXT_RADIUS: 80,
  STROKE_WIDTH: 1,
} as const;

export const SVG_CONFIG = {
  SIZE: 300,
  CENTER: 150,
  RADIUS: 140,
  STROKE_WIDTH: 1,
} as const;
