version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - cd frontend
      - npm install

  build:
    commands:
      - npm run build

  post_build:
    commands:
      - ls -al dist

artifacts:
  base-directory: frontend/dist
  files:
    - '**/*'

cache:
  paths:
    - 'node_modules/**/*'
