spring:
  data:
    redis:
      host: ${REDIS_HOST}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true

room:
  qr:
    prefix: "https://coffee-shout.com/join/"

websocket:
  docs:
    enabled: false
