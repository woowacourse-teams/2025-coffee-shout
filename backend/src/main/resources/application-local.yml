spring:
  cloud:
    aws:
      s3:
        enabled: false
  data:
    redis:
      port: 6379
      host: localhost

room:
  qr:
    prefix: http://localhost:8080/join/

logging:
  level:
    coffeeshout: DEBUG
    org.springframework.web: INFO
