logging:
  level:
    org.springframework.web.socket: DEBUG
    org.springframework.messaging: DEBUG
    coffeeshout: DEBUG

room:
  cleanup:
    interval: 500ms # 500밀리초 (테스트용)
    enabled: false  # 테스트에서는 스케줄러 비활성화
