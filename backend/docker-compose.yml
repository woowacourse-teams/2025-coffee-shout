version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: coffeeshout-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
